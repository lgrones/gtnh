{
  "rules": {
    "live": {
      "$graphId": {
        ".read": "auth != null",
        "updates": { ".write": "auth != null" },
        "presence": {
          "$uid": { ".write": "auth != null && auth.uid === $uid" }
        }
      }
    }
  }
}
